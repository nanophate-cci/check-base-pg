
version: 2.1
jobs:
  build:
    docker:
    - image: cimg/base:stable
    - image: cimg/postgres:11.17
      environment:
        PGDATA: /dev/shm/pgdata/data
    steps:
      - checkout
      - run: echo "hello wowwaaa"
